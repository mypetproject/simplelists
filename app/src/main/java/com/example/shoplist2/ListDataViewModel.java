package com.example.shoplist2;

import android.app.Application;

import androidx.lifecycle.AndroidViewModel;
import androidx.lifecycle.LiveData;

import java.util.List;

/*public class ListDataViewModel extends AndroidViewModel {

    private ListDataRepository mRepository;

    private LiveData<List<ListData>> mAllListData;

    public ListDataViewModel (Application application) {
        super(application);
        mRepository = new ListDataRepository(application);
        mAllListData = mRepository.getAllWords();
    }

    LiveData<List<ListData>> getAllListsData() { return mAllListData; }

    public void insert(ListData listData) { mRepository.insert(listData); }

}*/
